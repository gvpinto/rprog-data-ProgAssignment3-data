best <- function(state, outcome) {
    
    errorMsg <- paste(c('Error in best("', state, '", "', outcome, '") : invalid '), collapse="")
    
    if ("Heart Attack") {
        
    } else if("Heart Failure") {
        
    } else if ("pneumonia") {
        
    } else {
        paste(c(errorMsg, 'outcome', collapse="")
    }
    
}